#!/bin/bash
set -eu -o pipefail

case "$DISTRI" in
    debian)
        apt-get -y install libssl-dev
        ;;
    archlinux)
        pacman -Syu --noconfirm openssl
        ;;
    *)
        echo "Unsupported distribution"
        exit 1
        ;;
esac
