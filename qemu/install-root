#!/bin/bash -e

case "$DISTRI" in
    debian)
        ;;
    archlinux)
        pacman -Syu --noconfirm --needed python2
        ;;
    *)
        echo "Unsupported distribution"
        exit 1
        ;;
esac
